import { Bell, Lock, UserRound } from 'lucide-react';
import { useAuth } from '../context/AuthContext';
export default function Settings() { const { user } = useAuth(); return <div className="space-y-6"><div><p className="text-sm font-medium text-brand-600">Workspace</p><h1 className="page-title mt-1">Settings</h1><p className="page-subtitle">Manage your profile and workspace preferences.</p></div><div className="grid gap-6 lg:grid-cols-2"><div className="panel p-6"><div className="flex items-center gap-3 border-b border-slate-100 pb-5"><div className="rounded-xl bg-brand-50 p-3 text-brand-600"><UserRound size={20} /></div><div><h2 className="font-semibold">Profile</h2><p className="text-sm text-slate-400">Your account details</p></div></div><div className="space-y-4 pt-5"><div><p className="text-xs font-semibold uppercase tracking-wider text-slate-400">Name</p><p className="mt-1 font-medium">{user?.name}</p></div><div><p className="text-xs font-semibold uppercase tracking-wider text-slate-400">Email</p><p className="mt-1 font-medium">{user?.email}</p></div><div><p className="text-xs font-semibold uppercase tracking-wider text-slate-400">Role</p><p className="mt-1 font-medium">{user?.role}</p></div></div></div><div className="panel p-6"><div className="flex items-center gap-3 border-b border-slate-100 pb-5"><div className="rounded-xl bg-violet-50 p-3 text-violet-600"><Bell size={20} /></div><div><h2 className="font-semibold">Preferences</h2><p className="text-sm text-slate-400">Application settings</p></div></div><div className="space-y-4 pt-5"><div className="flex items-center justify-between"><div><p className="font-medium">Email notifications</p><p className="text-xs text-slate-400">Receive operational updates</p></div><div className="h-6 w-11 rounded-full bg-brand-600 p-1"><div className="ml-5 h-4 w-4 rounded-full bg-white" /></div></div><div className="flex items-center gap-3 rounded-xl bg-slate-50 p-4 text-sm text-slate-500"><Lock size={17} /> Authentication is secured with JWT.</div></div></div></div></div>; }
